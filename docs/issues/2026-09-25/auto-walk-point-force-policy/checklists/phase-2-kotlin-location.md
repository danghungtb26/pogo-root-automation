# Phase 2 — Kotlin admission và location ownership

Plan: [Tổng quan](00-overview.md)

Điều kiện bắt đầu: T-003 core policy và T-004/T-005 candidate wire đạt Verify.
Điều kiện hoàn tất: candidate event tới Kotlin qua router hiện có; policy quyết định trước location executor; Kotlin giữ accepted candidate và controller tick fake location từng bước tới target, trong khi owner/session/provider guards vẫn được giữ.

Sau mỗi task, kiểm tra diff và kết quả Verify, cập nhật ngay khối details dưới task trước khi chuyển tiếp. Chỉ tick `[x]` khi Verify đạt. Không tạo hoặc sửa test code theo `AGENTS.md`.

- [x] **T-006** **[app]** *(android)* — Thêm candidate receiver/coordinator trong `app/src/main/java/dev/pogoroot/automation/location/NativeWalkCandidateReceiver.kt` và nối `RuntimeUiEventRouter.kt`, `RuntimeUiStateStore.kt`, `RuntimeUiAutomationFacade.kt`, `HeadlessAutomationService.kt` tới event mới. Giữ runtime identity, sequence monotonic, timestamp freshness tại admission, reset/session guards, capability gate theo T-001 và tách trạng thái candidate khỏi navigation v1 cũ. **AC:** AC-01, AC-02, AC-03, AC-06, AC-07. **Phụ thuộc:** T-003, T-004, T-005. **Verify:** `./gradlew :app:testDebugUnitTest --tests dev.pogoroot.automation.runtime.NativeNavigationReceiverTest --rerun-tasks` (regression baseline; không cover candidate policy); source walkthrough event wrong capability/session/sequence/version/stale/future không publish target.

  <details>
  <summary>Đã hoàn thành — T-006</summary>

  - Tạo `NativeWalkCandidateReceiver.kt` và nối `HeadlessAutomationService` → `RuntimeUiAutomationFacade` → `RuntimeUiEventRouter` → receiver; thêm observation type 12 decode/capability gate. Luồng navigation v1 không đổi.
  - `RuntimeUiEventRouter` kiểm runtime identity/session, sequence tăng, timestamp freshness, hard payload size và capability trước khi dispatch. Cursor sequence được commit vào `RuntimeUiStateStore.lastObservationSeq` trước decode; vì vậy wrong-version, unsupported capability, malformed, ignored hoặc rejected payload không thể replay.
  - `NativeWalkCandidateReceiver` kiểm session/nonblank, candidate sequence so với cursor trước đó, duplicate sequence, timestamp freshness, rồi map WALK thành domain candidate hoặc STOP/ARRIVED thành terminal intent. Reset/session change clear cursor và gọi reset callback.
  - Theo review, bỏ raw candidate khỏi `RuntimeUiStateStore`; state chỉ phản ánh cursor đã qua ingress guards. Active candidate sẽ được lưu/render sau admission ở T-007, không trước policy decision.
  - Timestamp source đã đối chiếu: native `now_elapsed_nanos()` dùng `clock_gettime(CLOCK_MONOTONIC)`; router/receiver dùng `System.nanoTime()`, cùng monotonic domain Android. Source walkthrough: identity mismatch, stale/future, wrong capability/version, sequence lùi/duplicate không dispatch candidate; session reset gọi receiver reset.
  - Verify: `./gradlew test assembleDebug` exit 0; `:app:testDebugUnitTest` và `:app:assembleDebug` thành công. Test report `NativeNavigationReceiverTest.xml` ghi 5 tests, 0 failures/errors. Source walkthrough đã cover identity/session, wrong capability/version, sequence lùi/duplicate, stale/future timestamps, malformed payload, reset và không dispatch candidate trên guard failure. `git diff --check` exit 0; trailing-whitespace scan không match; mọi source dưới 500 dòng. Không tạo/sửa test code.

  </details>

- [x] **T-007** **[app]** *(android)* — Thêm active-route ownership/execution trong `app/src/main/java/dev/pogoroot/automation/location/WalkCandidateCoordinator.kt` [new] và tích hợp với `JoystickLocationController`, `JoystickOverlayService`, favorite/map-target và joystick entrypoints. Chỉ route được accept mới gọi `walkTo`; no-force B bị bỏ qua và A tiếp tục tick tới target; force replace chỉ khi owner policy cho phép; USER action preempt hoặc bảo vệ route theo contract; stale STOP/ARRIVED chỉ tác động đúng ID/generation. Expose `userWalkTo(target)` làm API cho route USER để UI sau này có thể nhập tọa độ mà reuse cùng coordinator/executor; không thêm input screen trong task này. Giữ controller làm single mock-location writer, không đưa candidate selection/game state vào đó. `JoystickOverlayService.kt` hiện 499 dòng; giữ ≤500 bằng delegation/extraction nếu cần. **AC:** AC-02…AC-06, AC-08, AC-09 (API reuse only). **Phụ thuộc:** T-003, T-006. **Verify:** `./gradlew :app:testDebugUnitTest --tests dev.pogoroot.automation.runtime.NativeNavigationReceiverTest --rerun-tasks` (regression baseline; không cover candidate policy); walkthrough A→B false, A→B true, same target, manual joystick/favorite/map target, typed target API, provider failure, session reset, local arrival; xác nhận local arrival không gọi native gameplay action.

  <details>
  <summary>Đã hoàn thành — T-007</summary>

  - Tạo `WalkCandidateCoordinator.kt` làm active-route owner và `WalkCandidateLocationActions.kt` làm adapter duy nhất từ coordinator tới `JoystickLocationController`; candidate chỉ gọi `walkTo` sau ACCEPTED/REPLACED. IGNORED_BUSY/REJECTED_OWNER_CONFLICT không chạm executor; UNCHANGED giữ route hiện có, không restart. Force vẫn bị USER owner từ chối. `userWalkTo(target)` là API chung; UI coordinate input tương lai chỉ cần parse/validate rồi gọi adapter này.
  - Nối `HeadlessAutomationService` receiver callbacks vào `submit`/`terminate`/`reset`. Native terminal chỉ match active `(sessionId, candidateId)`, rồi policy gắn generation hiện hành. Không có lease timer; ID đã active được tombstone trong session. Contract producer yêu cầu ID duy nhất/session; candidate bị ignore không bị tombstone.
  - Tích hợp USER joystick trong `MainOverlayView`, teleport dialog, favorite walk/teleport và map-target walk qua `WalkCandidateLocationActions`; các đường này preempt native ownership trước khi controller nhận lệnh. `JoystickLocationController` vẫn là mock-location writer duy nhất cho các đường generic. Legacy `AutoFortNavigationBus` listener vẫn dispatch trực tiếp ở phase này; giữ nguyên, việc migrate fort producer/consumer deferred tới T-008/T-009 như scope đã chốt.
  - `onLocationState` dọn route native khi arrival/error/provider không sẵn sàng. Error/not-ready clears owner rồi gọi `stopWalking`; callback lặp không gây stop lặp vì route đã clear. Local arrival chỉ kết thúc fake movement, không gọi gameplay/native action. Executor failure cũng fail-closed và dừng movement.
  - Review generation callback: `JoystickLocationState.walkGeneration` mang generation route do coordinator cấp; `JoystickLocationController.routeRevision` bắt mọi target/joystick/teleport/stop thay đổi. Tick kiểm tra revision trong cùng lock trước sink publish và trước khi apply arrival/error, do đó tick stale bị bỏ trước side effect; executor state callback chỉ có thể kết thúc route nếu generation khớp. Teleport publish callback cũng bỏ qua nếu revision đã đổi.
  - Walkthrough source: idle→candidate accept→một `walkTo` rồi controller tick dần tới target; A→B `force=false` giữ nguyên target/generation A và không gọi executor; A→B `force=true` thay A rồi gọi B; same-ID/target trả UNCHANGED, không restart tick; USER joystick/favorite/map/teleport preempt native và chặn candidate force; typed coordinate sau này gọi API USER hiện có; stale terminal A sau B không khớp; session reset invalidate native nhưng giữ generation tăng liên tục; provider failure clears/stops; local arrival chỉ clear route. Native receiver giữ sequence đã commit pre-decode, và coordinator dùng previous cursor để policy reject/accept đúng thứ tự.
  - Verify: `./gradlew test assembleDebug --rerun-tasks` exit 0, 51 tasks executed; `:app:testDebugUnitTest` và `:app:assembleDebug` thành công. Build phát các deprecation warnings Android đã có, không có lỗi Kotlin. Source walkthrough cover one-shot tick lifecycle, A→B false/true, same-ID UNCHANGED, USER preemption, API USER cho typed coordinate về sau, session reset không tái dùng generation, provider failure, stale terminal và local arrival. `git diff --check` exit 0. Chỉ còn `controller.walkTo` trực tiếp trong listener AutoFort legacy, được defer tới T-008/T-009. Không tạo/sửa test code.

  </details>
